{% extends 'base.html.twig' %}

{% block title %}PhpDeveloperLevelTestPassed index{% endblock %}

{% block body %}
    <h1>User: {{ user.username }}</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Test</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for php_developer_level_test_passed in alreadyPassed %}
            <tr>
                <td>{{ php_developer_level_test_passed.phpDeveloperLevelTest.title }}</td>
                <td>
                    <a href="{{ path('php_developer_level_test_passed_show', {'id': php_developer_level_test_passed.id}) }}">show</a>
                    <a href="{{ path('php_developer_level_test_passed_edit', {'id': php_developer_level_test_passed.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('php_developer_level_test_passed_new') }}">Create new</a>
{% endblock %}
