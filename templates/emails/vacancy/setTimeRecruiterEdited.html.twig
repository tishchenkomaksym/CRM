{% extends 'emails/vacancy/template.twig' %}

{% block text %}

    <p>
        Вы изменили дату/время интервью по Заявке№
            {{ candidateVacancy.vacancy.id }}
        с кандидатом {{ candidateVacancy.candidate.name }}
        {{ candidateVacancy.candidate.name }} на {{ candidateVacancy.dateInterview|date('Y-m-d') }} в
        {{ candidateVacancy.dateInterview|date('H:i:s') }} в {{ candidateVacancy.confRoom }}.
    </p>

{% endblock %}