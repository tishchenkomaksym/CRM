<form method="post" action="{{ path('qa_user_manager_relation_delete', {'id': qa_user_manager_relation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ qa_user_manager_relation.id) }}">
    <button class="btn">Delete</button>
</form>
