{% extends 'base.html.twig' %}

{% block title %}Approve candidate{% endblock %}

{% block breadcrumbs %}{% endblock %}
{% block menu %}{% endblock %}
{% block body %}
    <div class="d-flex flex-column align-items-center mt-5">
        <p>
            You send comment on Candidate for Request #{{ vacancy.id }}
        </p>
        <a class="btn btn-warning" href="{{ path('vacancy_show_approved_interview_department_manager_candidates', {'id': vacancy.id}) }}">
            View all candidates for approval
        </a>
        <a class="btn btn-primary mt-3" href="{{ path('vacancy_show_approved_interview_department_manager_comment_edit', {'id': commentViewer.id, 'vacancy': vacancy.id}) }}">Edit</a>
    </div>
{% endblock %}

