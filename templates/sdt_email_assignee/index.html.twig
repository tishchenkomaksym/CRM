{% extends 'base.html.twig' %}

{% block title %}SDTEmailAssignee index{% endblock %}

{% block body %}
    <h1>SDTEmailAssignee index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Email</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for sdt_email_assignee in sdt_email_assignees %}
            <tr>
                <td>{{ sdt_email_assignee.id }}</td>
                <td>{{ sdt_email_assignee.email }}</td>
                <td>
                    <a href="{{ path('sdt_email_assignee_show', {'id': sdt_email_assignee.id}) }}">show</a>
                    <a href="{{ path('sdt_email_assignee_edit', {'id': sdt_email_assignee.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('sdt_email_assignee_new',{'id':user.id}) }}">Create new</a>
{% endblock %}
